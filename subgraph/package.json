{
  "name": "ctx_delegation_stats",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ctx_delegation_stats",
    "create-local": "graph create --node http://localhost:8020/ ctx_delegation_stats",
    "remove-local": "graph remove --node http://localhost:8020/ ctx_delegation_stats",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ctx_delegation_stats",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
